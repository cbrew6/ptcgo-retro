# Extracted game data - derived from the local PTCGO install, not ours to
# redistribute. Regenerate with the exporter (see CLAUDE.md).
carddata/

# Asset-name index extracted from the installed .unity3d bundles.
# Regenerate with: python bundle_index.py
bundle_index.json

# Self-signed TLS key/cert - regenerate locally, never commit a private key.
certs/

# Per-machine state
accounts.json
server.log
*.log

__pycache__/
*.pyc

# Build artifacts / third-party tooling for the loose-art patch
patch/*.dll
patch/cecil/
tools/setcache/
tools/fetch_all_art.log
tools/art_state.json
tools/namecache/
tools/art_provenance.json
decks.json
decks.json.bak

# Metadata extracted from donated caches - TPCI's data, not ours to
# redistribute. Regenerate with: python tools/import_donor_metadata.py <cache>
donor/
